export const HOST_PATH = `http://${window.location.host}/api`;
export const GAME_URL = `ws://${window.location.host}/ws/game-server/`;
export const ITEM_URL = `ws://${window.location.host}/ws/item-server/`;
export const MESSAGE_URL = `ws://${window.location.host}/ws/message-server/`;